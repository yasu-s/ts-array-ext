{
  "private": true,
  "version": "0.0.0",
  "name": "example-typescript",
  "dependencies": {
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.0",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.0",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "test": "jest",
    "test:w": "jest --watch",
    "test:wa": "jest --watchAll"
  },
  "jest": {
    "testEnvironment": "node",
    "verbose": true,
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    },
    "globals": {
      "ts-jest": {
        "tsConfig": "tsconfig.json"
      }
    },
    "testMatch": [
      "**/test/*.+(ts|tsx|js)"
    ],
    "resetMocks": true,
    "setupFilesAfterEnv": [
      "./setup-clock-patch.js"
    ]
  }
}
